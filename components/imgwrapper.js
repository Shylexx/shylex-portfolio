const ImageCenter = ({ children }) => {
    return (
        <div style={{ justifyContent: "center", display: "flex", marginBottom: "10px" }}>
            {children}
        </div>
    )
}

export default ImageCenter
